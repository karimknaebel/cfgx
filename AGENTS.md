## Docs
Docs live in `docs/` and are built via Zensical.

## Versioning
We use SemVer. In particular, while this library is in 0.x.x we will do breaking changes without migration path or deprecation behavior on minor releases.
